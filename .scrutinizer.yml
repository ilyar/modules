build:
    dependencies:
        before:
            - npm install
    tests:
        override:
            -
                command: 'nyc --reporter=clover npm run bdd'
                coverage:
                    file: 'coverage/clover.xml'
                    format: 'clover'
checks:
    javascript: true
filter:
    excluded_paths:
        - "tests/"
